# // TODO: figure out how to read and save JSON
# TODO: include logging message when a field is not included in the JSON that it will use default values.
# Or specify that JSON is mal-formed.
import json

# def config_json(**kwargs):
    # arglist = zip(kwargs)
    # projectConfigFields = {'sublist': dict, 'contrasts': list, 'codePaths': dict}
    # analysisConfigFields = {'sublist': dict, 'contrasts': list, 'paths': dict}
    # inputPathConfigFields = {'sublist': dict, '': list, 'picklePaths': dict}
    # writePathConfigFields = {}
    #
    # projectConfig = defaultdict('sublist': [], 'contrasts': [])
    # analysisConfig = defaultdict()
    # inputPathConfig = defaultdict()
    # writePathConfig = defaultdict()

